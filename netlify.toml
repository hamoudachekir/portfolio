[build]
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"

# Form handling with email notifications
[[forms]]
  [forms.contact]
    fields = ["name", "email", "message"]
